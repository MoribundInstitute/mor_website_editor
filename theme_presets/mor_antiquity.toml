name = "Mor Antiquity"
description = "An engraved celestial plate: sepia ink on aged parchment, ruled with a gold ecliptic and a verdigris graticule, set in Fell types beneath Roman capitals. Toggles to a star-flecked astronomer's nocturne. Inks: gold folio, verdigris, oxblood, indigo atlas."

preset_css = '''
/* ============================================================
   Mor Antiquity  —  "Folio / Nocturne"
   The page as a leaf from a 17th-century star atlas: an off-
   centre ecliptic struck in gold, a fine verdigris coordinate
   grid, panels as pressed paper plates with double-ruled
   borders. Dark mode is the same plate printed at night —
   sepia-black ink and a scatter of gilt stars.
   Display: Cinzel (Roman inscriptional capitals).
   Body: IM Fell English (Oxford Fell types).
   Verdigris constant: #4a6153 (oxidized brass instruments).
   ============================================================ */

/* ---- The celestial ground: ecliptic rings over an engraver's graticule ---- */
body::before { background: none !important; opacity: 0 !important; }
html, body { background-color: var(--bg-base) !important; }
body {
  min-height: 100vh;
  background-image:
    /* concentric ecliptic rings, gold, struck from an off-centre pole */
    repeating-radial-gradient(circle at 50% 34%,
      transparent 0 77px,
      color-mix(in srgb, var(--accent) 10%, transparent) 77px 78px,
      transparent 78px 156px),
    /* fine verdigris graticule — the chart's coordinate grid */
    repeating-linear-gradient(0deg,
      transparent 0 63px, color-mix(in srgb, #4a6153 7%, transparent) 63px 64px),
    repeating-linear-gradient(90deg,
      transparent 0 63px, color-mix(in srgb, #4a6153 7%, transparent) 63px 64px),
    /* plate vignette + paper wash */
    radial-gradient(120% 120% at 50% 0%, transparent 55%,
      color-mix(in srgb, var(--fg-base) 12%, transparent) 100%),
    linear-gradient(180deg,
      color-mix(in srgb, var(--bg-base) 96%, var(--fg-base)) 0%,
      var(--bg-base) 45%) !important;
  background-attachment: fixed !important;
}

/* Nocturne only: a scatter of gilt stars behind the plate. */
html:not([data-theme="light"]) body::after {
  content: "";
  position: fixed;
  inset: 0;
  z-index: -1;
  pointer-events: none;
  background-image:
    radial-gradient(circle at 22% 28%, color-mix(in srgb, var(--accent) 55%, transparent) 0 1px, transparent 1.6px),
    radial-gradient(circle at 68% 12%, color-mix(in srgb, var(--fg-base) 40%, transparent) 0 1px, transparent 1.5px),
    radial-gradient(circle at 84% 62%, color-mix(in srgb, var(--accent) 38%, transparent) 0 1.3px, transparent 2px),
    radial-gradient(circle at 40% 78%, color-mix(in srgb, var(--fg-base) 30%, transparent) 0 1px, transparent 1.4px),
    radial-gradient(circle at 8% 66%, color-mix(in srgb, var(--accent) 30%, transparent) 0 1px, transparent 1.5px);
  background-size: 340px 340px, 260px 260px, 420px 420px, 300px 300px, 380px 380px;
}

/* ---- Panels as pressed paper plates: hairline gold edge, double rule ---- */
.canvas-core, .mor-post, .mor-panel, .main-header, .mor-footer {
  background: color-mix(in srgb, var(--bg-panel) 96%, var(--accent)) !important;
  border: 1px solid color-mix(in srgb, var(--accent) 40%, var(--border-color)) !important;
  border-radius: 2px !important;
  box-shadow:
    inset 0 0 0 1px color-mix(in srgb, var(--bg-base) 50%, transparent),
    inset 0 0 0 4px color-mix(in srgb, var(--accent) 8%, transparent),
    0 10px 30px color-mix(in srgb, var(--fg-base) 10%, transparent) !important;
}

/* ---- Inscriptional headings: Roman capitals, letterspaced ---- */
.mor-post h1, .main-header h1, .mor-brand-name, .mor-masthead-title {
  font-family: var(--font-heading) !important;
  font-variant: small-caps;
  letter-spacing: 0.045em !important;
}

/* Signature divider: a constellation node — hairline broken by a gold star. */
.mor-post h1::after, .mor-masthead-title::after {
  content: "";
  display: block;
  height: 9px;
  margin-top: 0.55rem;
  background:
    radial-gradient(circle at 50% 50%, var(--accent) 0 2px, transparent 2.5px),
    linear-gradient(90deg, transparent 0,
      color-mix(in srgb, var(--accent) 55%, transparent) 12%,
      color-mix(in srgb, var(--accent) 55%, transparent) 88%, transparent 100%);
  background-repeat: no-repeat;
  background-position: center, center;
  background-size: 100% 100%, 100% 1px;
}

/* The same node, standing alone, whenever prose calls for a rule. */
hr {
  border: none !important;
  width: 62%;
  height: 11px;
  margin: 2.2rem auto;
  background:
    radial-gradient(circle at 50% 50%, var(--accent) 0 2px, transparent 2.5px),
    linear-gradient(90deg, transparent 0,
      color-mix(in srgb, var(--accent) 50%, transparent) 15%,
      color-mix(in srgb, var(--accent) 50%, transparent) 85%, transparent 100%);
  background-repeat: no-repeat;
  background-position: center, center 50%;
  background-size: 100% 100%, 100% 1px;
}

/* One book flourish: an illuminated capital in a ruled woodcut box. */
.mor-post > p:first-of-type::first-letter {
  float: left;
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 3em;
  line-height: 0.72;
  margin: 0.06em 0.16em 0 0;
  padding: 0.1em 0.16em;
  color: var(--accent);
  border: 1px solid color-mix(in srgb, var(--accent) 45%, transparent);
}

/* ---- Gilded ink links: replace the harsh inversion hover ---- */
a {
  color: color-mix(in srgb, var(--accent) 70%, var(--fg-base));
  border-bottom: 1px dotted color-mix(in srgb, var(--accent) 55%, transparent);
}
a:hover, a:focus-visible {
  background: color-mix(in srgb, var(--accent) 14%, transparent);
  color: var(--fg-base);
  border-bottom: 1px solid var(--accent);
}
::selection {
  background: color-mix(in srgb, var(--accent) 32%, var(--bg-base));
  color: var(--fg-base);
}

/* ---- Nav: engraved small-caps, verdigris resting, gold on hover ---- */
.mor-nav > a {
  font-family: var(--font-heading);
  font-variant: small-caps;
  letter-spacing: 0.06em;
  color: color-mix(in srgb, var(--fg-base) 74%, var(--bg-base)) !important;
  border-bottom-color: transparent;
  transition: color 180ms ease;
}
.mor-nav > a:hover, .mor-nav > a:focus-visible {
  background: transparent;
  color: var(--accent) !important;
  border-bottom-color: var(--accent);
}

/* ---- Colophon lines: eyebrow, footer strip in engraved small caps ---- */
.mor-masthead-eyebrow, .mor-footer-strip p, .mor-footer-hairline p {
  font-variant: small-caps;
  letter-spacing: 0.05em;
  color: color-mix(in srgb, var(--fg-muted) 85%, var(--accent)) !important;
}
.mor-pill {
  font-variant: small-caps;
  letter-spacing: 0.05em;
  padding: 0.05em 0.5em;
  border: 1px solid color-mix(in srgb, var(--accent) 40%, transparent);
  border-radius: 2px;
}

/* ---- Marginalia: quoted text in italic Fell with a verdigris rule ---- */
blockquote {
  border-left: 2px solid color-mix(in srgb, var(--accent) 55%, #4a6153) !important;
  margin-left: 0;
  padding: 0.35rem 1.1rem !important;
  font-style: italic;
  color: color-mix(in srgb, var(--fg-base) 86%, var(--bg-base)) !important;
  background: color-mix(in srgb, var(--accent) 5%, transparent);
}

/* ---- Plate captions: code set apart on an inset paper panel ---- */
code, kbd, samp {
  font-size: 0.92em;
  padding: 0.08em 0.35em;
  background: color-mix(in srgb, var(--fg-base) 8%, transparent);
  border: 1px solid color-mix(in srgb, var(--border-color) 60%, transparent);
  border-radius: 2px;
}
pre {
  padding: 1rem 1.25rem;
  overflow: auto;
  background: color-mix(in srgb, var(--bg-base) 55%, var(--bg-panel));
  border: 1px solid color-mix(in srgb, var(--accent) 30%, var(--border-color));
  border-left: 2px solid color-mix(in srgb, var(--accent) 55%, #4a6153);
}
pre code { padding: 0; background: none; border: none; }

/* ---- Figures mounted as plates: mat, hairline gold frame ---- */
.mor-post img {
  background: var(--bg-elevated);
  border: 1px solid color-mix(in srgb, var(--accent) 42%, var(--border-color));
  padding: 6px;
  box-shadow: 0 4px 14px color-mix(in srgb, var(--fg-base) 12%, transparent);
}

/* ---- Sidebar plate headers: engraved small caps over a hairline rule ---- */
.mor-panel summary, .mor-toc-title, .mor-footer h3 {
  font-family: var(--font-heading) !important;
  font-variant: small-caps;
  letter-spacing: 0.08em !important;
  border-bottom: 1px solid color-mix(in srgb, var(--accent) 35%, transparent) !important;
}

/* ---- Tables ruled like an ephemeris: heavy gold head rule, hairline rows ---- */
.mor-post table {
  border-collapse: collapse;
  width: 100%;
}
.mor-post th {
  font-variant: small-caps;
  letter-spacing: 0.05em;
  text-align: left;
  padding: 0.3em 0.7em;
  border-bottom: 2px solid color-mix(in srgb, var(--accent) 70%, transparent);
}
.mor-post td {
  padding: 0.3em 0.7em;
  border-bottom: 1px solid color-mix(in srgb, var(--fg-base) 14%, transparent);
}

/* ---- Instruments in hand: dip-pen nib, pointing manicule, serif I-beam ---- */
:root {
  --theme-cursor-default: url("data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' width='24' height='24' viewBox='0 0 24 24'><path d='M3 2L6 2L16 16L13 19L11 17L3 2Z' fill='{{ACCENT_URLENCODED}}' stroke='%232a2012' stroke-width='1'/><path d='M9 11L13 15' stroke='%232a2012' stroke-width='1'/></svg>") 3 2, auto;
  --theme-cursor-pointer: url("data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' width='24' height='24' viewBox='0 0 24 24'><path d='M9 2.2c-.7 0-1.3.6-1.3 1.3v6.5H6.9c-.6 0-1.1.3-1.4.8-.3.5-.2 1.1.1 1.6l2.2 3.1c.5.7 1.3 1.1 2.1 1.1h3.4c1 0 1.9-.7 2.1-1.7l.7-3.2c.2-.9-.3-1.8-1.2-2-.2 0-.3-.1-.5-.1h-2.8V3.5c0-.7-.6-1.3-1.3-1.3z' fill='{{ACCENT_URLENCODED}}' stroke='%232a2012' stroke-width='1'/></svg>") 9 2, pointer;
}
input, textarea { cursor: url("data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' width='24' height='24' viewBox='0 0 24 24'><path d='M9 3h6M12 3v18M9 21h6' fill='none' stroke='{{ACCENT_URLENCODED}}' stroke-width='1.6'/></svg>") 12 12, text !important; }
'''

# --- Base / light: aged parchment ("Folio") ---
[colors]
bg_base = "#e8dcc0"
fg_base = "#2b2113"
fg_muted = "#6d5a3a"
accent = "#8a6413"
border = "#c3a86c"
panel_border_width = "1px"
glow_spread = "14px"
hover_scale = "1.01"

[colors.bg_panel]
mode = "Solid"
color = "#f2e8cf"

[colors.bg_elevated]
mode = "Solid"
color = "#f8f0dc"

[background.mode.Solid]
color = "#e8dcc0"

[typography]
body_font_stack = "'IM Fell English', Georgia, 'Times New Roman', serif"
heading_font_stack = "'Cinzel', 'Playfair Display', Georgia, serif"
mono_font_stack = "'Inconsolata', monospace"
base_size = "18px"
scale_ratio = "1.333"
line_height = "1.72"
heading_weight = "600"

[buttons]
radius = "2px"
border_width = "1px"
border_style = "solid"
text_transform = "uppercase"
letter_spacing = "0.09em"
padding_x = "1rem"
padding_y = "0.5rem"
font_weight = "600"
fill = "gradient"
gradient_from = "color-mix(in srgb, var(--accent) 24%, var(--bg-panel))"
gradient_to = "color-mix(in srgb, var(--accent) 10%, var(--bg-base))"
gradient_angle = "180deg"
border_color = "var(--accent)"
text_color = "var(--fg-base)"
box_shadow = "inset 0 1px 0 color-mix(in srgb, var(--bg-base) 70%, transparent), 0 2px 4px color-mix(in srgb, var(--fg-base) 18%, transparent)"
elevation = "subtle"
hover_effect = "lift"
transition_ms = "160ms"
easing = "cubic-bezier(0.2, 0.6, 0.2, 1)"
pressed_feedback = true

# --- Light toggle: the same parchment plate, stated explicitly ---
[light.colors]
bg_base = "#e8dcc0"
fg_base = "#2b2113"
fg_muted = "#6d5a3a"
accent = "#8a6413"
border = "#c3a86c"
panel_border_width = "1px"
glow_spread = "14px"
hover_scale = "1.01"

[light.colors.bg_panel]
mode = "Solid"
color = "#f2e8cf"

[light.colors.bg_elevated]
mode = "Solid"
color = "#f8f0dc"

[light.background.mode.Solid]
color = "#e8dcc0"

# --- Dark toggle: astronomer's night ink ("Nocturne") ---
[dark.colors]
bg_base = "#15110a"
fg_base = "#e6dfc8"
fg_muted = "#a3957a"
accent = "#d4af37"
border = "#3b321f"
panel_border_width = "1px"
glow_spread = "14px"
hover_scale = "1.01"

[dark.colors.bg_panel]
mode = "Solid"
color = "#201a10"

[dark.colors.bg_elevated]
mode = "Solid"
color = "#2a2214"

[dark.background.mode.Solid]
color = "#15110a"

[scrollbars]
width = "12px"
track_color = "color-mix(in srgb, var(--fg-base) 7%, var(--bg-base))"
thumb_color = "var(--accent)"
thumb_hover_color = "color-mix(in srgb, var(--accent) 75%, var(--fg-base))"

# --- Color variants: different inks on matching papers -----------------------
# Each scheme re-tints the paper and the night plate to sit under its ink, so
# a swap reads as a different edition of the atlas, not a hue-rotated page.

[[variants]]
name = "Verdigris"
[variants.light]
colors = { bg_base = "#e4ddc6", fg_base = "#242419", fg_muted = "#5d6350", accent = "#2e7d6a", border = "#a9b895", bg_panel = { mode = "Solid", color = "#efe9d4" }, bg_elevated = { mode = "Solid", color = "#f5f1e0" } }
background = { mode = { Solid = { color = "#e4ddc6" } } }
[variants.dark]
colors = { bg_base = "#0e1410", fg_base = "#dde3d1", fg_muted = "#93a08a", accent = "#58a98c", border = "#29443a", bg_panel = { mode = "Solid", color = "#16201a" }, bg_elevated = { mode = "Solid", color = "#1e2b23" } }
background = { mode = { Solid = { color = "#0e1410" } } }

[[variants]]
name = "Oxblood"
[variants.light]
colors = { bg_base = "#eaddc6", fg_base = "#2e1e15", fg_muted = "#71564a", accent = "#7d261c", border = "#c4a48e", bg_panel = { mode = "Solid", color = "#f4e8d6" }, bg_elevated = { mode = "Solid", color = "#f9f0e2" } }
background = { mode = { Solid = { color = "#eaddc6" } } }
[variants.dark]
colors = { bg_base = "#170e0b", fg_base = "#e8ddce", fg_muted = "#a8917f", accent = "#c96a52", border = "#46281f", bg_panel = { mode = "Solid", color = "#221410" }, bg_elevated = { mode = "Solid", color = "#2c1a15" } }
background = { mode = { Solid = { color = "#170e0b" } } }

[[variants]]
name = "Indigo Atlas"
[variants.light]
colors = { bg_base = "#e2e0d0", fg_base = "#1f2433", fg_muted = "#4f586d", accent = "#23487d", border = "#a9b0be", bg_panel = { mode = "Solid", color = "#edeadb" }, bg_elevated = { mode = "Solid", color = "#f4f1e4" } }
background = { mode = { Solid = { color = "#e2e0d0" } } }
[variants.dark]
colors = { bg_base = "#0c111e", fg_base = "#d9deea", fg_muted = "#8b94ab", accent = "#8aa8dc", border = "#2a3550", bg_panel = { mode = "Solid", color = "#141b2c" }, bg_elevated = { mode = "Solid", color = "#1c2438" } }
background = { mode = { Solid = { color = "#0c111e" } } }
